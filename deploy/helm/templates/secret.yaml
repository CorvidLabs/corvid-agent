apiVersion: v1
kind: Secret
metadata:
  name: {{ include "corvid-agent.fullname" . }}-secrets
  labels:
    {{- include "corvid-agent.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{- if $value }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  {{- end }}
