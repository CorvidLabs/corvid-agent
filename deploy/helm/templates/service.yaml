apiVersion: v1
kind: Service
metadata:
  name: {{ include "corvid-agent.fullname" . }}
  labels:
    {{- include "corvid-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "corvid-agent.selectorLabels" . | nindent 4 }}
