apiVersion: v1
kind: Service
metadata:
  name: corvid-agent
  namespace: corvid-agent
  labels:
    app.kubernetes.io/name: corvid-agent
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: corvid-agent
