/**
 * @deprecated This file has been split into focused test modules.
 *
 * See:
 *   - discord-commands-autocomplete.test.ts  — handleAutocomplete
 *   - discord-commands-components.test.ts    — handleComponentInteraction
 *   - discord-commands-info.test.ts          — info commands (agents, status, dashboard, etc.)
 *   - discord-commands-moderation.test.ts    — handleMute, handleUnmute, handleCouncil
 *   - discord-commands-session.test.ts       — handleSession, handleWork
 *   - discord-commands-dispatch.test.ts      — handleInteraction routing + permission middleware
 */
